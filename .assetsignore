# Files in the repo that should NOT be served from the edge.
# wrangler reads this when building the static-assets bundle.
README.md
wrangler.jsonc
.assetsignore
node_modules
.github
.git

# Render-only HTML used to generate the committed PNG screenshots.
# Not linked, not in the sitemap, and should never be indexed.
mockups
shot-swarm-source.html
